# arm_tooling_core

`arm_tooling_core` contains the shared pure-Dart ARM primitives used by both
the Flutter client SDK and the server-side ARM SDK.

It owns:

- capture severity and breadcrumb models
- capture request/result contracts
- fingerprint generation
- case, issue, and session ID generation
- payload sanitization helpers
- shared issue/case Firestore document builders
