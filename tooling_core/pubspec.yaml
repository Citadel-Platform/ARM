name: arm_tooling_core
description: Shared pure-Dart primitives and document contracts for the Citadel ARM SDK.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.4

dependencies:
  crypto: ^3.0.7
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
