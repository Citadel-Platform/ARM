{
  "name": "@citadel/arm-web",
  "version": "0.1.0",
  "description": "ARM for the browser, on the Citadel Core SDK: uncaught errors, unhandled rejections, failed requests, resource and CSP failures, sent to the shared ARM ingest with Core's breadcrumbs and session.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "prebuild": "npm --prefix ../../platform/web_core run build && npm --prefix ../contract/js run build",
    "build": "tsc -p tsconfig.json",
    "bundle": "npm run build && node tool/bundle.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.14.5",
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.4.4"
  },
  "dependencies": {
    "@citadel/core-web": "file:../../platform/web_core",
    "@citadel/arm-contract": "file:../contract/js"
  },
  "files": [
    "dist/src",
    "dist/browser",
    "README.md"
  ]
}
